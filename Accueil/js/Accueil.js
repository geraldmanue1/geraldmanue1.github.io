$(function () {
  $(".text").typed({
    strings: ["Qu'est ce que un BTS SIO ?", "", "Definir le métier", "Quelle est mon métier plus tard ?"],
    typeSpeed: 70,
    backSpeed: 20,
    backDelay: 1500,
    showCursor: true,
    loop: true
  });
});
